# winemac-crossprocess-child-core.patch — cross-process child window Metal swapchains (stock wine).
#
# WARNING: AI-AUTHORED. Read this before you read the code.
#
# This patch was written with heavy AI assistance. 3Shain/dxmt's CONTRIBUTING.md cannot accept
# AI-authored contributions, so it was never offered as a PR; the findings went upstream as reports
# instead: https://github.com/3Shain/dxmt/issues/141#issuecomment-5477055980 and
# https://bugs.winehq.org/show_bug.cgi?id=60263. It is published here on purpose. If you are a wine
# or dxmt maintainer who intends to write your own implementation, reading this file is a choice
# with a consequence: the reports carry the whole diagnosis with none of the code.
#
# LICENSING. The four files this touches are LGPL-2.1-or-later with CodeWeavers copyright lines,
# and a unified diff of them reproduces LGPL context and removed lines. The modifications are
# (c) 2026 the author and are offered under LGPL-2.1-or-later, the license of the files they
# modify; context lines are Wine's and are not relicensed. This repository's own scripts and docs
# remain MIT.
#
# APPLY: to a PRISTINE wine 11.16 tree, from the tree root:
#   patch -p1 < winemac-crossprocess-child-core.patch
# It also applies cleanly on top of scripts/wineandaqua-dxmt.patch — the insertion points are
# deliberately placed away from that patch's. Requires wine >= 11.11.
# This half implements the FIXME for every caller, including vulkan.c, and depends on nothing
# from DXMT. On its own it changes no behaviour for an application that never asks for a
# cross-process child swapchain.
#
# Generated by scripts/regen-winemac-patches.sh — do not hand-edit; regenerate.
diff --git a/dlls/winemac.drv/cocoa_window.m b/dlls/winemac.drv/cocoa_window.m
index 8ca5dd7..10a8a26 100644
--- a/dlls/winemac.drv/cocoa_window.m
+++ b/dlls/winemac.drv/cocoa_window.m
@@ -406,8 +406,12 @@ - (void) wine_getBackingSize:(int*)outBackingSize;
     - (void) wine_setBackingSize:(const int*)newBackingSize;
 
     - (WineMetalView*) newMetalViewWithDevice:(id<MTLDevice>)device;
-    - (void) addCALayerHostViewWithContextId:(CAContextID)contextId;
+    - (void) addCALayerHostViewWithContextId:(CAContextID)contextId frame:(CGRect)frame;
+    - (void) updateCALayerHostFrame:(CAContextID)contextId frame:(CGRect)frame;
+    - (void) setCALayerHostZPosition:(CAContextID)contextId zPosition:(double)zpos;
     - (void) removeCALayerHostView:(CAContextID)contextId;
+    - (void) placeCALayerHost:(CALayerHost*)host contextId:(CAContextID)contextId frame:(CGRect)frame;
+    - (void) getCALayerHostPlacement:(CAContextID)contextId scale:(double[2])scale content:(CGSize*)content;
 
 @end
 
@@ -496,6 +500,46 @@ - (NSFocusRingType) focusRingType
 @end
 
 
+/* Win32 speaks raw pixels and Cocoa points -- two to one on retina -- so an odd pixel dimension
+ * is a half point, and a layer that Win32 says reaches the window edge stops one device pixel
+ * short of it, showing the window beneath as a white hairline. Snapped only at the edge, so an
+ * interior sibling keeps its exact geometry and two widgets can never be made to overlap. */
+static CGRect snap_host_frame_to_view_edges(CGRect frame, CGRect view)
+{
+    /* one device pixel in points; a fixed 1.0 would also close a gap the app asked for */
+    const CGFloat px = retina_on ? 0.5 : 1.0;
+
+    if (CGRectIsEmpty(frame) || CGRectIsEmpty(view)) return frame;
+
+    if (frame.origin.x > 0.0 && frame.origin.x <= px)
+    {
+        frame.size.width += frame.origin.x;
+        frame.origin.x = 0.0;
+    }
+    if (frame.origin.y > 0.0 && frame.origin.y <= px)
+    {
+        frame.size.height += frame.origin.y;
+        frame.origin.y = 0.0;
+    }
+    if (CGRectGetMaxX(frame) >= view.size.width - px)
+        frame.size.width = view.size.width - frame.origin.x;
+    if (CGRectGetMaxY(frame) >= view.size.height - px)
+        frame.size.height = view.size.height - frame.origin.y;
+    return frame;
+}
+
+/* Per-host geometry, keyed by context id and read together with _caLayerHosts. The content size is
+ * what a host's remote tree was created at, and is what lets a host whose content is stale be
+ * SCALED to its new frame instead of exposing background along the growing edge. The target frame
+ * replaces a CGRectEqualToRect against host.frame, which on a transformed layer reads back as a
+ * float product and compares as noise. */
+@interface WineContentView ()
+
+@property (retain, nonatomic) NSMutableDictionary<NSNumber*, NSValue*>* caLayerHostContentSizes;
+@property (retain, nonatomic) NSMutableDictionary<NSNumber*, NSValue*>* caLayerHostTargetFrames;
+
+@end
+
 @implementation WineContentView
 
 @synthesize everHadGLContext = _everHadGLContext;
@@ -519,6 +563,8 @@ - (void) dealloc
         [glContexts release];
         [pendingGlContexts release];
         [_caLayerHosts release];
+        [_caLayerHostContentSizes release];
+        [_caLayerHostTargetFrames release];
         CGImageRelease(colorImage);
         CGImageRelease(shapeImage);
         [super dealloc];
@@ -722,10 +768,71 @@ - (WineMetalView*) newMetalViewWithDevice:(id<MTLDevice>)device
         return _metalView;
     }
 
-    - (void) addCALayerHostViewWithContextId:(CAContextID)contextId
+    /* One place computes a host's geometry, because frame on a CALayer is DERIVED from bounds,
+     * anchorPoint, position and transform (CALayer.h) and is undefined to set while a transform is
+     * applied. The bounds stay at the size the remote content was created at, so a frame that no
+     * longer matches is reached by SCALING -- a stale swapchain is stretched over the whole frame
+     * instead of leaving the uncovered remainder showing the host's background, which is the black
+     * strip along the growing edge of a window being dragged larger. masksToBounds clips in layer
+     * space, so a scaled host still clips at exactly its scaled frame. */
+    - (void) placeCALayerHost:(CALayerHost*)host contextId:(CAContextID)contextId frame:(CGRect)frame
+    {
+        /* (0,0) is the min-x/min-y corner of bounds (CALayer.h), which is the visual top-left only
+         * in flipped geometry. Measured, not assumed: the spike that established a hosted tree
+         * honours transform at all printed geometryFlipped=YES for this view first. */
+        host.anchorPoint = CGPointMake(0.0, 0.0);
+
+        if (CGRectIsEmpty(frame))
+        {
+            /* 0x0 is not a scale of anything. Same result as the frame = CGRectZero this replaces,
+             * plus an explicit identity so a host that returns to a real size is not left scaled by
+             * a stale factor. */
+            host.bounds = CGRectZero;
+            host.position = frame.origin;
+            host.transform = CATransform3DIdentity;
+            [_caLayerHostTargetFrames setObject:[NSValue valueWithRect:frame] forKey:@(contextId)];
+            return;
+        }
+
+        NSValue* stored = [_caLayerHostContentSizes objectForKey:@(contextId)];
+        CGSize content = stored ? stored.sizeValue : frame.size;
+        /* One device pixel in points, the same tolerance the edge snap uses and for the same
+         * reason: snap_host_frame_to_view_edges extends a frame by up to px only when it reaches
+         * the CURRENT view edge, so once the root grows, the same child rect comes back up to half
+         * a point narrower than the size stored from the snapped one. An exact compare would leave
+         * a scale of 0.9998 -- and a filtered blur -- on every page of any host that never
+         * recreates. The background covers a seam that small. */
+        const CGFloat px = retina_on ? 0.5 : 1.0;
+        /* A few pixels is not a base to scale from. Chromium really does create 1x1 widgets, and
+         * stretching one over a whole window paints a single pixel's colour across everything
+         * below it -- worse than the gap this exists to close. Measured: a churn produced two
+         * frames at scale 1920 x 907 before this floor was added. Below it, do what the code did
+         * before: place at identity and let the background show. */
+        if (content.width < 8.0 || content.height < 8.0 ||
+            (fabs(frame.size.width  - content.width)  <= px &&
+             fabs(frame.size.height - content.height) <= px))
+            content = frame.size;
+
+        host.bounds = (CGRect){ CGPointZero, content };
+        host.position = frame.origin;
+        host.transform = CGSizeEqualToSize(content, frame.size)
+                       ? CATransform3DIdentity
+                       : CATransform3DMakeScale(frame.size.width  / content.width,
+                                                frame.size.height / content.height, 1.0);
+        /* A scaled layer antialiases its own edges by default, which would put a hairline at the
+         * window edge -- exactly the seam snap_host_frame_to_view_edges exists to close. */
+        host.edgeAntialiasingMask = 0;
+        [_caLayerHostTargetFrames setObject:[NSValue valueWithRect:frame] forKey:@(contextId)];
+    }
+
+    - (void) addCALayerHostViewWithContextId:(CAContextID)contextId frame:(CGRect)frame
     {
         if (!_caLayerHosts)
+        {
             _caLayerHosts = [[NSMutableDictionary alloc] init];
+            _caLayerHostContentSizes = [[NSMutableDictionary alloc] init];
+            _caLayerHostTargetFrames = [[NSMutableDictionary alloc] init];
+        }
 
         [self removeCALayerHostView:contextId];
 
@@ -733,8 +840,33 @@ - (void) addCALayerHostViewWithContextId:(CAContextID)contextId
         [host setContextId:contextId];
         host.magnificationFilter = kCAFilterNearest;
         host.contentsScale = retina_on ? 2.0 : 1.0;
-        host.frame = self.layer.bounds;
-        host.autoresizingMask = kCALayerWidthSizable | kCALayerHeightSizable;
+        frame = snap_host_frame_to_view_edges(frame, self.layer.bounds);
+        /* A host given a real child rect is positioned and clipped to it, so siblings on one
+         * window stop occluding one another. Three cases, not two: CGRectIsEmpty cannot tell "no
+         * rect supplied" from "really 0x0", and a 0x0 layer stretched over the content view with
+         * nothing drawn in it blacks out everything below -- a window with no area is hidden. */
+        if (CGRectIsNull(frame))
+        {
+            /* no child rect supplied at all -- the root-window case */
+            host.frame = self.layer.bounds;
+            host.autoresizingMask = kCALayerWidthSizable | kCALayerHeightSizable;
+        }
+        else if (CGRectIsEmpty(frame))
+        {
+            host.frame = CGRectZero;
+            host.masksToBounds = YES;
+            host.hidden = YES;
+        }
+        else
+        {
+            host.masksToBounds = YES;
+            /* The creation rect IS this host's content size for its whole life: the remote content
+             * only changes size through a recreate, and a recreate arrives as a new context id
+             * with a new host, the old one retired at CREATE. */
+            [_caLayerHostContentSizes setObject:[NSValue valueWithSize:frame.size]
+                                         forKey:@(contextId)];
+            [self placeCALayerHost:host contextId:contextId frame:frame];
+        }
 
         [self.layer addSublayer:host];
         [_caLayerHosts setObject:host forKey:@(contextId)];
@@ -742,11 +874,72 @@ - (void) addCALayerHostViewWithContextId:(CAContextID)contextId
         [(WineWindow*)self.window windowDidDrawContent];
     }
 
+    - (void) updateCALayerHostFrame:(CAContextID)contextId frame:(CGRect)frame
+    {
+        CALayerHost* host = [_caLayerHosts objectForKey:@(contextId)];
+        frame = snap_host_frame_to_view_edges(frame, self.layer.bounds);
+        if (host && CGRectIsEmpty(frame) && !CGRectIsNull(frame))
+        {
+            /* Became 0x0 while hosted: left alone, the layer keeps its last size and still covers
+             * live content. Zeroing the frame as well as hiding is the net below's other half. */
+            [CATransaction begin];
+            [CATransaction setDisableActions:YES];
+            host.hidden = YES;
+            [self placeCALayerHost:host contextId:contextId frame:CGRectZero];
+            [CATransaction commit];
+        }
+        else if (host && !CGRectIsEmpty(frame))
+        {
+            /* The layer sits in the owner's coordinate space and the child it mirrors can move
+             * or resize in the other process, so refresh rather than leave a stale frame.
+             * Visibility and geometry are independent state and are tested separately: a window
+             * that reports 0x0 and returns to its old size would stay hidden under one guard. */
+            [CATransaction begin];
+            [CATransaction setDisableActions:YES];
+            NSValue* last = [_caLayerHostTargetFrames objectForKey:@(contextId)];
+            if (!last || !CGRectEqualToRect(last.rectValue, frame))
+                [self placeCALayerHost:host contextId:contextId frame:frame];
+            if (host.hidden) host.hidden = NO;
+            [CATransaction commit];
+        }
+    }
+
+    /* Stack the hosted layers the way the windows they mirror stack: insertion order is not that
+     * order, since a client that recreates a swapchain on every resize puts the last-recreated
+     * layer on top of live content. zPosition is authoritative whenever a layer was added. */
+    /* What a host ended up at, for the caller's TRACE. Reads the layer rather than recomputing, so
+     * a scale that was never applied cannot be reported as one that was. */
+    - (void) getCALayerHostPlacement:(CAContextID)contextId scale:(double[2])scale content:(CGSize*)content
+    {
+        CALayerHost* host = [_caLayerHosts objectForKey:@(contextId)];
+        NSValue* stored = [_caLayerHostContentSizes objectForKey:@(contextId)];
+        if (scale)
+        {
+            scale[0] = host ? host.transform.m11 : 0.0;
+            scale[1] = host ? host.transform.m22 : 0.0;
+        }
+        if (content) *content = stored ? stored.sizeValue : CGSizeZero;
+    }
+
+    - (void) setCALayerHostZPosition:(CAContextID)contextId zPosition:(double)zpos
+    {
+        CALayerHost* host = [_caLayerHosts objectForKey:@(contextId)];
+        if (host && host.zPosition != zpos)
+        {
+            [CATransaction begin];
+            [CATransaction setDisableActions:YES];
+            host.zPosition = zpos;
+            [CATransaction commit];
+        }
+    }
+
     - (void) removeCALayerHostView:(CAContextID)contextId
     {
         NSNumber* key = @(contextId);
         [[_caLayerHosts objectForKey:key] removeFromSuperlayer];
         [_caLayerHosts removeObjectForKey:key];
+        [_caLayerHostContentSizes removeObjectForKey:key];
+        [_caLayerHostTargetFrames removeObjectForKey:key];
     }
 
     - (void) setLayerRetinaProperties:(BOOL)mode
@@ -790,6 +983,12 @@ - (void) setRetinaMode:(BOOL)mode
 
         for (CALayerHost* host in [_caLayerHosts allValues])
             host.contentsScale = mode ? 2.0 : 1.0;
+        /* Both are in points and every surviving host's scale has just flipped, so a stored value
+         * is 2x off in one direction or the other. Dropped rather than converted: the next reframe
+         * stores the current rect, and until then a host is placed at identity -- which is exactly
+         * what it did before any of this. */
+        [_caLayerHostContentSizes removeAllObjects];
+        [_caLayerHostTargetFrames removeAllObjects];
 
         [super setRetinaMode:mode];
     }
@@ -4080,24 +4279,26 @@ - (void) dealloc
 @interface CAContextSwapChain : NSObject <WineMetalSwapChain>
 {
     void* hwnd;
+    void* child_hwnd;          /* the cross-process child this layer belongs to (may be NULL) */
     macdrv_metal_device device;
     CAMetalLayer* offscreen_layer;
     CAContext* remote_context;
     CAContextID context_id;
 }
 
-- (instancetype) initWithHwnd:(void*)hwnd bounds:(CGRect)bounds;
+- (instancetype) initWithHwnd:(void*)hwnd child:(void*)child bounds:(CGRect)bounds;
 
 @end
 
 @implementation CAContextSwapChain
 
-- (instancetype) initWithHwnd:(void*)newHwnd bounds:(CGRect)bounds
+- (instancetype) initWithHwnd:(void*)newHwnd child:(void*)newChild bounds:(CGRect)bounds
 {
     self = [super init];
     if (!self) return nil;
 
     hwnd = newHwnd;
+    child_hwnd = newChild;
     if (!(device = macdrv_create_metal_device()))
     {
         [self release];
@@ -4136,7 +4337,7 @@ - (instancetype) initWithHwnd:(void*)newHwnd bounds:(CGRect)bounds
         return nil;
     }
 
-    macdrv_create_remote_layer(hwnd, context_id);
+    macdrv_create_remote_layer(hwnd, child_hwnd, context_id);
     return self;
 }
 
@@ -4169,9 +4370,10 @@ macdrv_metal_swapchain macdrv_create_view_swapchain(macdrv_view v)
     return (macdrv_metal_swapchain)[[MetalViewSwapChain alloc] initWithView:v];
 }
 
-macdrv_metal_swapchain macdrv_create_offscreen_swapchain(void* hwnd, CGRect bounds)
+macdrv_metal_swapchain macdrv_create_offscreen_swapchain(void* hwnd, void* child, CGRect bounds)
 {
-    return (macdrv_metal_swapchain)[[CAContextSwapChain alloc] initWithHwnd:hwnd bounds:bounds];
+    return (macdrv_metal_swapchain)[[CAContextSwapChain alloc] initWithHwnd:hwnd child:child
+                                                                        bounds:bounds];
 }
 
 macdrv_metal_layer macdrv_swapchain_get_layer(macdrv_metal_swapchain swapchain)
@@ -4185,7 +4387,35 @@ void macdrv_destroy_swapchain(macdrv_metal_swapchain swapchain)
     [(id<WineMetalSwapChain>)swapchain release];
 }
 
-void macdrv_window_create_ca_layer_host_view(macdrv_window w, unsigned int context_id)
+void macdrv_window_create_ca_layer_host_view(macdrv_window w, unsigned int context_id,
+                                             CGRect frame, double zpos, int zpos_valid)
+{
+@autoreleasepool
+{
+    WineWindow* window = (WineWindow*)w;
+
+    OnMainThread(^{
+        NSView* content_view = [window contentView];
+
+        /* The frame arrives in Win32 raw pixels and CALayer.frame is in points -- window.c works
+         * in Win32 space and the Cocoa side converts. A null frame (no child rect) stays null: it
+         * selects the whole-view case below and must not go through the retina divide. */
+        if ([content_view isKindOfClass:[WineContentView class]])
+        {
+            CGRect mac_frame = CGRectIsNull(frame) ? CGRectNull : cgrect_mac_from_win(frame);
+
+            [(WineContentView*)content_view addCALayerHostViewWithContextId:context_id
+                                                                      frame:mac_frame];
+            if (zpos_valid)
+                [(WineContentView*)content_view setCALayerHostZPosition:context_id zPosition:zpos];
+        }
+    });
+}
+}
+
+void macdrv_window_update_ca_layer_host_frame(macdrv_window w, unsigned int context_id,
+                                              CGRect frame, double zpos, int zpos_valid,
+                                              double scale_out[2], CGSize *content_out)
 {
 @autoreleasepool
 {
@@ -4194,8 +4424,17 @@ void macdrv_window_create_ca_layer_host_view(macdrv_window w, unsigned int conte
     OnMainThread(^{
         NSView* content_view = [window contentView];
 
+        /* same Win32-pixels -> Cocoa-points conversion as the create path above */
         if ([content_view isKindOfClass:[WineContentView class]])
-            [(WineContentView*)content_view addCALayerHostViewWithContextId:context_id];
+        {
+            CGRect mac_frame = CGRectIsNull(frame) ? CGRectNull : cgrect_mac_from_win(frame);
+
+            [(WineContentView*)content_view updateCALayerHostFrame:context_id frame:mac_frame];
+            if (zpos_valid)
+                [(WineContentView*)content_view setCALayerHostZPosition:context_id zPosition:zpos];
+            [(WineContentView*)content_view getCALayerHostPlacement:context_id
+                                                              scale:scale_out content:content_out];
+        }
     });
 }
 }
diff --git a/dlls/winemac.drv/macdrv.h b/dlls/winemac.drv/macdrv.h
index aeeadae..b9e4b3f 100644
--- a/dlls/winemac.drv/macdrv.h
+++ b/dlls/winemac.drv/macdrv.h
@@ -185,6 +185,9 @@ extern BOOL macdrv_SystemParametersInfo(UINT action, UINT int_param, void *ptr_p
     struct window_rects rects;                  /* window rects in monitor DPI, relative to parent client area */
     int                 pixel_format;           /* pixel format for GL */
     HANDLE              drag_event;             /* event to signal that Cocoa-driven window dragging has ended */
+    CFMutableDictionaryRef remote_layer_children; /* CAContextID -> child HWND, for repositioning
+                                                   * hosted cross-process layers on resize */
+    unsigned int        in_live_resize : 1;     /* WINDOW_FRAME_CHANGED.in_resize; cleared by WINDOW_RESIZE_ENDED */
     unsigned int        on_screen : 1;          /* is window ordered in? (minimized or not) */
     unsigned int        shaped : 1;             /* is window using a custom region shape? */
     unsigned int        layered : 1;            /* is window layered and with valid attributes? */
diff --git a/dlls/winemac.drv/macdrv_cocoa.h b/dlls/winemac.drv/macdrv_cocoa.h
index 9a91edb..0c54423 100644
--- a/dlls/winemac.drv/macdrv_cocoa.h
+++ b/dlls/winemac.drv/macdrv_cocoa.h
@@ -529,12 +529,20 @@ extern void macdrv_order_cocoa_window(macdrv_window w, macdrv_window prev,
 extern macdrv_metal_layer macdrv_view_get_metal_layer(macdrv_metal_view v);
 extern void macdrv_view_release_metal_view(macdrv_metal_view v);
 extern macdrv_metal_swapchain macdrv_create_view_swapchain(macdrv_view v);
-extern macdrv_metal_swapchain macdrv_create_offscreen_swapchain(void* hwnd, CGRect bounds);
+extern macdrv_metal_swapchain macdrv_create_offscreen_swapchain(void* hwnd, void* child,
+                                                                 CGRect bounds);
 extern macdrv_metal_layer macdrv_swapchain_get_layer(macdrv_metal_swapchain swapchain);
 extern void macdrv_destroy_swapchain(macdrv_metal_swapchain swapchain);
-extern void macdrv_window_create_ca_layer_host_view(macdrv_window w, unsigned int context_id);
+extern void macdrv_window_create_ca_layer_host_view(macdrv_window w, unsigned int context_id,
+                                                    CGRect frame, double zpos, int zpos_valid);
 extern void macdrv_window_release_ca_layer_host_view(macdrv_window w, unsigned int context_id);
-extern void macdrv_create_remote_layer(void* hwnd, unsigned int context_id);
+/* scale_out and content_out are optional and report what the hosting layer ended up with: the
+ * factor its remote content is being stretched by, and the size that content was created at. The
+ * Cocoa side has no debug channel of its own, so this is how the geometry reaches a TRACE. */
+extern void macdrv_window_update_ca_layer_host_frame(macdrv_window w, unsigned int context_id,
+                                                     CGRect frame, double zpos, int zpos_valid,
+                                                     double scale_out[2], CGSize *content_out);
+extern void macdrv_create_remote_layer(void* hwnd, void* child, unsigned int context_id);
 extern void macdrv_release_remote_layer(void* hwnd, unsigned int context_id);
 extern bool macdrv_get_view_backing_size(macdrv_view v, int backing_size[2]);
 extern void macdrv_set_view_backing_size(macdrv_view v, const int backing_size[2]);
diff --git a/dlls/winemac.drv/window.c b/dlls/winemac.drv/window.c
index c23142d..b605b87 100644
--- a/dlls/winemac.drv/window.c
+++ b/dlls/winemac.drv/window.c
@@ -39,6 +39,103 @@
 
 WINE_DEFAULT_DEBUG_CHANNEL(macdrv);
 
+/* Win32 paint order for the hosted children of one root; higher index = nearer the viewer.
+ * Siblings are walked bottom to top (GW_HWNDNEXT runs top to bottom) and a window is recorded
+ * before its own children, so a child sits above its parent and an upper sibling above the whole
+ * subtree of a lower one. Built once per update, not once per layer: this runs during a drag. */
+#define PAINT_ORDER_DEPTH 32
+
+struct paint_order
+{
+    HWND *hwnds;                   /* index 0 = furthest back; grown by doubling */
+    int   count, capacity;
+    BOOL  truncated;               /* a bound was hit; z-order below is incomplete */
+};
+
+/* An unlogged bound reads as "we ordered everything" when we did not; once per process, though,
+ * since this runs on the drag path. Layers past it keep their previous z. */
+static void paint_order_truncated(struct paint_order *po, const char *why, HWND at)
+{
+    static BOOL warned;
+
+    if (!warned)
+    {
+        FIXME("%s below %p -- paint order incomplete, hosted layers past the cap keep their z\n",
+              why, at);
+        warned = TRUE;
+    }
+    po->truncated = TRUE;
+}
+
+static BOOL paint_order_add(struct paint_order *po, HWND hwnd)
+{
+    if (po->count == po->capacity)
+    {
+        int capacity = po->capacity ? po->capacity * 2 : 32;
+        HWND *hwnds = realloc(po->hwnds, capacity * sizeof(*hwnds));
+
+        if (!hwnds)
+        {
+            paint_order_truncated(po, "out of memory", hwnd);
+            return FALSE;
+        }
+        po->hwnds = hwnds;
+        po->capacity = capacity;
+    }
+    po->hwnds[po->count++] = hwnd;
+    return TRUE;
+}
+
+/* GW_CHILD gives the topmost child and GW_HWNDLAST the bottommost sibling, so GW_HWNDPREV from
+ * there walks bottom to top. Recording a window before descending into it puts a child above its
+ * parent and an upper sibling above the whole subtree of a lower one, which is Win32 paint order
+ * -- in one pass, with no per-level array and no bound on breadth. */
+static void paint_order_walk(HWND parent, struct paint_order *po, int depth)
+{
+    HWND k;
+
+    if (depth >= PAINT_ORDER_DEPTH)
+    {
+        paint_order_truncated(po, "window tree too deep", parent);
+        return;
+    }
+
+    if (!(k = NtUserGetWindowRelative(parent, GW_CHILD))) return;
+
+    for (k = NtUserGetWindowRelative(k, GW_HWNDLAST); k;
+         k = NtUserGetWindowRelative(k, GW_HWNDPREV))
+    {
+        if (!paint_order_add(po, k)) return;
+        paint_order_walk(k, po, depth + 1);
+    }
+}
+
+static void build_paint_order(HWND root, struct paint_order *po)
+{
+    po->hwnds = NULL;
+    po->count = po->capacity = 0;
+    po->truncated = FALSE;
+    if (root) paint_order_walk(root, po, 0);
+}
+
+static void paint_order_free(struct paint_order *po)
+{
+    free(po->hwnds);
+    po->hwnds = NULL;
+    po->count = po->capacity = 0;
+}
+
+/* TRUE + *zpos on success; on failure the caller must leave zPosition alone. Returning 0.0 for
+ * "not found" would be indistinguishable from a legitimate bottom-most position. */
+static BOOL paint_order_zpos(const struct paint_order *po, HWND child, double *zpos)
+{
+    int i;
+
+    for (i = 0; i < po->count; i++)
+        if (po->hwnds[i] == child) { *zpos = (double)(i + 1); return TRUE; }
+    return FALSE;
+}
+
 
 static pthread_mutex_t win_data_mutex;
 static CFMutableDictionaryRef win_datas;
@@ -843,6 +940,35 @@ static HMONITOR monitor_from_point(POINT pt, UINT flags)
 }
 
 
+/* Retire every other hosted layer mirroring the same child, called once the replacement is in
+ * the layer tree. Retiring on the old swapchain's death instead leaves the child unhosted until
+ * the replacement's own message arrives, and the window shows straight through for that long. */
+static void retire_superseded_layers(struct macdrv_win_data *data, HWND child, unsigned int keep)
+{
+    CFIndex count, i;
+    const void **keys, **vals;
+
+    if (!data->remote_layer_children || !child) return;
+    if (!(count = CFDictionaryGetCount(data->remote_layer_children))) return;
+
+    keys = calloc(count, sizeof(*keys));
+    vals = calloc(count, sizeof(*vals));
+    if (!keys || !vals) { free(keys); free(vals); return; }
+    CFDictionaryGetKeysAndValues(data->remote_layer_children, keys, vals);
+
+    for (i = 0; i < count; i++)
+    {
+        unsigned int ctx = (unsigned int)(UINT_PTR)keys[i];
+
+        if (ctx == keep || (HWND)vals[i] != child) continue;
+        TRACE("retiring superseded layer ctx %u for child %p (replaced by %u)\n", ctx, child, keep);
+        if (data->cocoa_window) macdrv_window_release_ca_layer_host_view(data->cocoa_window, ctx);
+        CFDictionaryRemoveValue(data->remote_layer_children, (const void *)(UINT_PTR)ctx);
+    }
+    free(keys);
+    free(vals);
+}
+
 /***********************************************************************
  *              move_window
  *
@@ -1178,14 +1304,34 @@ BOOL macdrv_client_surface_acquire_metal_swapchain(struct macdrv_client_surface
     {
         RECT rect;
 
-        if (NtUserGetAncestor(hwnd, GA_ROOT) != hwnd)
-        {
-            FIXME("Cross-process child window Metal swapchains are not implemented\n");
-            return FALSE;
-        }
+        HWND root = NtUserGetAncestor(hwnd, GA_ROOT);
 
         if (!NtUserGetClientRect(hwnd, &rect, NtUserGetWinMonitorDpi(hwnd, MDT_RAW_DPI))) return FALSE;
-        surface->metal_swapchain = macdrv_create_offscreen_swapchain(hwnd, cgrect_from_rect(rect));
+
+        if (root && root != hwnd)
+        {
+            /* Cross-process child window: it has no cocoa_window of its own, so the CAContext
+             * is posted to its root, which owns an NSWindow and lives in the other process. The
+             * frame goes in the root's space -- the child's own client rect is (0,0,w,h) and
+             * would put every hosted layer at the root's origin -- computed as the update path
+             * computes it, so creation and every later move agree. */
+            RECT child_rect = rect, root_rect;
+            UINT geom_dpi = NtUserGetWinMonitorDpi(hwnd, MDT_RAW_DPI);
+
+            if (NtUserGetWindowRect(hwnd, &child_rect, geom_dpi) &&
+                NtUserGetWindowRect(root, &root_rect, geom_dpi))
+                OffsetRect(&child_rect, -root_rect.left, -root_rect.top);
+            else
+                child_rect = rect;   /* fall back to the old behaviour rather than guess */
+
+            TRACE("cross-process child %p -> root %p frame %s\n", hwnd, root,
+                  wine_dbgstr_rect(&child_rect));
+            surface->metal_swapchain =
+                macdrv_create_offscreen_swapchain(root, hwnd, cgrect_from_rect(child_rect));
+        }
+        else
+            surface->metal_swapchain =
+                macdrv_create_offscreen_swapchain(hwnd, NULL, cgrect_from_rect(rect));
     }
 
     return surface->metal_swapchain != NULL;
@@ -1269,6 +1415,9 @@ void macdrv_DestroyWindow(HWND hwnd)
 
     if (!(data = get_win_data(hwnd))) return;
 
+    if (data->remote_layer_children)
+        CFRelease(data->remote_layer_children);
+
     if (data->drag_event) NtSetEvent(data->drag_event, NULL);
 
     destroy_cocoa_window(data);
@@ -1557,8 +1706,57 @@ LRESULT macdrv_WindowMessage(HWND hwnd, UINT msg, WPARAM wp, LPARAM lp)
     case WM_MACDRV_CREATE_REMOTE_LAYER:
         if ((data = get_win_data(hwnd)))
         {
-            TRACE("WM_MACDRV_CREATE_REMOTE_LAYER context_id %u\n", (unsigned int)lp);
-            if (data->cocoa_window) macdrv_window_create_ca_layer_host_view(data->cocoa_window, (unsigned int)lp);
+            HWND child = (HWND)wp;
+            CGRect frame = CGRectNull;
+            BOOL host = TRUE;
+
+            TRACE("WM_MACDRV_CREATE_REMOTE_LAYER child %p context_id %u\n", child,
+                  (unsigned int)lp);
+            if (child)
+            {
+                UINT dpi = NtUserGetWinMonitorDpi(hwnd, MDT_RAW_DPI);
+                RECT cr, rr;
+
+                if (NtUserGetWindowRect(child, &cr, dpi) && NtUserGetWindowRect(hwnd, &rr, dpi))
+                {
+                    OffsetRect(&cr, -rr.left, -rr.top);
+                    frame = CGRectMake(cr.left, cr.top, cr.right - cr.left, cr.bottom - cr.top);
+                    TRACE("child %p layer frame in root %p = %s\n", child, hwnd,
+                          wine_dbgstr_rect(&cr));
+                }
+                else
+                {
+                    /* the child went away between the post and this handler; with no rect the
+                     * layer would cover the whole content view with nothing ever drawn into it */
+                    TRACE("child %p gone before context %u could be hosted -- not hosting\n",
+                          child, (unsigned int)lp);
+                    host = FALSE;
+                }
+            }
+            if (host && data->cocoa_window)
+            {
+                struct paint_order po;
+                double zpos = 0.0;
+                BOOL have_z;
+
+                build_paint_order(hwnd, &po);
+                have_z = child && paint_order_zpos(&po, child, &zpos);
+                paint_order_free(&po);
+                macdrv_window_create_ca_layer_host_view(data->cocoa_window, (unsigned int)lp, frame,
+                                                       zpos, have_z);
+                /* Track every hosted layer: a child's so it can be repositioned on resize, and
+                 * a top-level's own (child NULL) so the release handler can tell "never tracked"
+                 * from "already retired". */
+                if (!data->remote_layer_children)
+                    data->remote_layer_children = CFDictionaryCreateMutable(NULL, 0, NULL, NULL);
+                if (data->remote_layer_children)
+                {
+                    CFDictionarySetValue(data->remote_layer_children,
+                                         (const void *)(UINT_PTR)(unsigned int)lp, child);
+                    /* the replacement is hosted now -- only now do its predecessors go */
+                    if (child) retire_superseded_layers(data, child, (unsigned int)lp);
+                }
+            }
             release_win_data(data);
         }
         return 0;
@@ -1566,7 +1764,18 @@ LRESULT macdrv_WindowMessage(HWND hwnd, UINT msg, WPARAM wp, LPARAM lp)
         if ((data = get_win_data(hwnd)))
         {
             TRACE("WM_MACDRV_RELEASE_REMOTE_LAYER context_id %u\n", (unsigned int)lp);
-            if (data->cocoa_window) macdrv_window_release_ca_layer_host_view(data->cocoa_window, (unsigned int)lp);
+            /* Every hosted layer is tracked, so an untracked id is one retire_superseded_layers()
+             * already took, and skipping it keeps the two paths from racing over a reused id. */
+            if (!data->remote_layer_children ||
+                CFDictionaryContainsKey(data->remote_layer_children,
+                                        (const void *)(UINT_PTR)(unsigned int)lp))
+            {
+                if (data->cocoa_window)
+                    macdrv_window_release_ca_layer_host_view(data->cocoa_window, (unsigned int)lp);
+                if (data->remote_layer_children)
+                    CFDictionaryRemoveValue(data->remote_layer_children,
+                                            (const void *)(UINT_PTR)(unsigned int)lp);
+            }
             release_win_data(data);
         }
         return 0;
@@ -1577,9 +1786,11 @@ LRESULT macdrv_WindowMessage(HWND hwnd, UINT msg, WPARAM wp, LPARAM lp)
 }
 
 
-void macdrv_create_remote_layer(void* hwnd_ptr, unsigned int context_id)
+void macdrv_create_remote_layer(void* hwnd_ptr, void* child_ptr, unsigned int context_id)
 {
-    NtUserPostMessage((HWND)hwnd_ptr, WM_MACDRV_CREATE_REMOTE_LAYER, 0, context_id);
+    /* wParam carries the cross-process child hwnd (0 for a root window) so the owning process
+     * can position the hosted layer; lParam is the CAContext id. */
+    NtUserPostMessage((HWND)hwnd_ptr, WM_MACDRV_CREATE_REMOTE_LAYER, (WPARAM)child_ptr, context_id);
 }
 
 
@@ -1635,6 +1846,176 @@ BOOL macdrv_GetWindowStyleMasks(HWND hwnd, UINT style, UINT ex_style, UINT *styl
 /***********************************************************************
  *              WindowPosChanged   (MACDRV.@)
  */
+
+/* Re-place the cross-process layers hosted on this window: they mirror children owned by another
+ * process, which are not managed windows, so no WindowPosChanged of their own ever arrives. */
+/* The context id of this root's hosted layer for this child, or 0. Same value scan
+ * update_remote_layer_frames does, without doing the work when the answer is no. */
+static unsigned int remote_layer_context_for(struct macdrv_win_data *data, HWND child)
+{
+    CFIndex count, i;
+    const void **keys, **vals;
+    unsigned int context_id = 0;
+
+    if (!data->remote_layer_children || !child) return 0;
+    if (!(count = CFDictionaryGetCount(data->remote_layer_children))) return 0;
+
+    keys = calloc(count, sizeof(*keys));
+    vals = calloc(count, sizeof(*vals));
+    if (keys && vals)
+    {
+        CFDictionaryGetKeysAndValues(data->remote_layer_children, keys, vals);
+        for (i = 0; i < count && !context_id; i++)
+            if ((HWND)vals[i] == child) context_id = (unsigned int)(UINT_PTR)keys[i];
+    }
+    free(keys);
+    free(vals);
+    return context_id;
+}
+
+/* The frame a hosted layer should take, in the root's window-relative space: the child's own rect
+ * offset into the root, and -- during a live resize only -- the full-client substitution below.
+ *
+ * Both update passes go through here because the swapchain-creation comment above demands that
+ * creation and every later move compute one frame. `old_rects` is the root's rects as they were
+ * BEFORE the move being processed; the pass has no memory of its own, so it must be handed in, and
+ * a caller with nothing to compare against passes NULL and gets the plain offset.
+ *
+ * Returns FALSE when the child is gone, which the callers treat as "retire this layer". */
+static BOOL remote_layer_target_rect(struct macdrv_win_data *data, HWND child, UINT dpi,
+                                     const RECT *root_rect, const struct window_rects *old_rects,
+                                     RECT *target)
+{
+    RECT cr;
+
+    if (!NtUserGetWindowRect(child, &cr, dpi)) return FALSE;
+    OffsetRect(&cr, -root_rect->left, -root_rect->top);
+
+    if (old_rects && data->in_live_resize)
+    {
+        /* B': a child that filled the client area before this move is stretched with the root now,
+         * before the app resizes it; its own WindowPosChanged (D1) re-frames it after. Compared
+         * relative to the root, never in screen space -- a root move shifts every child's screen
+         * rect while its parent-relative rect is unchanged, so a screen-space compare would never
+         * match during a top- or left-edge drag. */
+        RECT old_client = old_rects->client, new_client = data->rects.client;
+
+        OffsetRect(&old_client, -old_rects->window.left, -old_rects->window.top);
+        OffsetRect(&new_client, -data->rects.window.left, -data->rects.window.top);
+        if (EqualRect(&cr, &old_client))
+        {
+            TRACE("child %p stretched %s -> %s (live resize)\n", child,
+                  wine_dbgstr_rect(&cr), wine_dbgstr_rect(&new_client));
+            cr = new_client;
+        }
+        else
+        {
+            /* Both rects, every time the substitution declines, because "no full-client child" and
+             * "the two rects are in different DPI spaces" are otherwise the same silence -- and
+             * these rects come from different queries: cr from MDT_RAW_DPI window rects, client
+             * from data->rects, documented as monitor DPI. A drag that produces no stretch must
+             * say which of the two it was. */
+            TRACE("child %p not full-client: %s vs client %s -- no stretch\n", child,
+                  wine_dbgstr_rect(&cr), wine_dbgstr_rect(&old_client));
+        }
+    }
+
+    *target = cr;
+    return TRUE;
+}
+
+/* Reposition ONE hosted layer. A child's own move only moves that child, and refreshing every
+ * layer on the root instead costs O(n) per child: during a resize the root moves, all n children
+ * move with it, and n full passes run where one pass would do. Measured on Steam's store page,
+ * that multiplier produced 9 near-black frames in 400 during churn where the same build without
+ * this path produced 0 in 320. The root's own WindowPosChanged still does the full pass. */
+static void update_remote_layer_frame_for(struct macdrv_win_data *data, HWND child,
+                                          unsigned int context_id)
+{
+    struct paint_order order;
+    RECT root_rect, cr;
+    double zpos = 0.0, scale[2] = { 0.0, 0.0 };
+    CGSize content = { 0.0, 0.0 };
+    BOOL have_z;
+    UINT dpi;
+
+    if (!data->cocoa_window || !child || !context_id) return;
+    dpi = NtUserGetWinMonitorDpi(data->hwnd, MDT_RAW_DPI);
+    if (!NtUserGetWindowRect(data->hwnd, &root_rect, dpi)) return;
+    if (!remote_layer_target_rect(data, child, dpi, &root_rect, NULL, &cr)) return;
+
+    build_paint_order(data->hwnd, &order);
+    have_z = paint_order_zpos(&order, child, &zpos);
+    paint_order_free(&order);
+
+    macdrv_window_update_ca_layer_host_frame(data->cocoa_window, context_id,
+        CGRectMake(cr.left, cr.top, cr.right - cr.left, cr.bottom - cr.top), zpos, have_z,
+        scale, &content);
+    TRACE("child %p context %u frame %s zpos %.0f%s creation %.0fx%.0f scale %.3f,%.3f\n",
+          child, context_id, wine_dbgstr_rect(&cr), zpos, have_z ? "" : " (not in paint order)",
+          content.width, content.height, scale[0], scale[1]);
+}
+
+static void update_remote_layer_frames(struct macdrv_win_data *data,
+                                       const struct window_rects *old_rects)
+{
+    CFIndex count, i;
+    const void **keys, **vals;
+    RECT root_rect;
+    UINT dpi;
+    struct paint_order order;
+
+    if (!data->remote_layer_children || !data->cocoa_window) return;
+    if (!(count = CFDictionaryGetCount(data->remote_layer_children))) return;
+
+    dpi = NtUserGetWinMonitorDpi(data->hwnd, MDT_RAW_DPI);
+    if (!NtUserGetWindowRect(data->hwnd, &root_rect, dpi)) return;
+
+    /* one walk for all of this window's hosted layers, not one per layer */
+    build_paint_order(data->hwnd, &order);
+
+    keys = calloc(count, sizeof(*keys));
+    vals = calloc(count, sizeof(*vals));
+    if (!keys || !vals) { free(keys); free(vals); paint_order_free(&order); return; }
+    CFDictionaryGetKeysAndValues(data->remote_layer_children, keys, vals);
+
+    for (i = 0; i < count; i++)
+    {
+        HWND child = (HWND)vals[i];
+        unsigned int context_id = (unsigned int)(UINT_PTR)keys[i];
+        RECT cr;
+
+        if (!child) continue;   /* a top-level's own layer fills the content view and autoresizes */
+        if (!remote_layer_target_rect(data, child, dpi, &root_rect, old_rects, &cr))
+        {
+            /* the child is gone; left alone its layer would stay painted at its last frame */
+            TRACE("child %p gone -- releasing hosted layer %u\n", child, context_id);
+            macdrv_window_release_ca_layer_host_view(data->cocoa_window, context_id);
+            CFDictionaryRemoveValue(data->remote_layer_children,
+                                    (const void *)(UINT_PTR)context_id);
+            continue;
+        }
+        {
+            double zpos = 0.0;
+            BOOL have_z = paint_order_zpos(&order, child, &zpos);
+
+            double scale[2] = { 0.0, 0.0 };
+            CGSize content = { 0.0, 0.0 };
+
+            macdrv_window_update_ca_layer_host_frame(data->cocoa_window, context_id,
+                CGRectMake(cr.left, cr.top, cr.right - cr.left, cr.bottom - cr.top),
+                zpos, have_z, scale, &content);
+            TRACE("child %p context %u frame %s zpos %.0f%s creation %.0fx%.0f scale %.3f,%.3f\n",
+                  child, context_id, wine_dbgstr_rect(&cr), zpos,
+                  have_z ? "" : " (not in paint order)",
+                  content.width, content.height, scale[0], scale[1]);
+        }
+    }
+    free(keys);
+    free(vals);
+    paint_order_free(&order);
+}
+
 void macdrv_WindowPosChanged(HWND hwnd, HWND insert_after, HWND owner_hint, UINT swp_flags,
                              const struct window_rects *new_rects, struct window_surface *surface)
 {
@@ -1654,7 +2035,30 @@ void macdrv_WindowPosChanged(HWND hwnd, HWND insert_after, HWND owner_hint, UINT
     TRACE("win %p/%p new_rects %s style %08x flags %08x surface %p\n", hwnd, data->cocoa_window,
           debugstr_window_rects(new_rects), new_style, swp_flags, surface);
 
-    if (!data->cocoa_window) goto done;
+    if (!data->cocoa_window)
+    {
+        /* A cross-process child has win_data but no cocoa_window of its own, and this hook runs in
+         * the thread that owns it -- the same thread that owns its root and hosts its layer, since
+         * a foreign SetWindowPos is marshalled here via WM_WINE_SETWINDOWPOS. So the owner already
+         * sees every child move and, before this branch, discarded it: a move or a z change with
+         * the root stationary left the hosted layer at its old rect until the root next moved. It
+         * only went unnoticed because the client re-creates its swapchain on every resize.
+         * swp_flags here are the effective flags after fixup_swp_flags, so a no-op call already
+         * carries NOMOVE|NOSIZE|NOZORDER and costs nothing. */
+        HWND root = NtUserGetAncestor(hwnd, GA_ROOT);
+        BOOL moved = (~swp_flags & (SWP_NOMOVE | SWP_NOSIZE | SWP_NOZORDER)) ||
+                     (swp_flags & (SWP_SHOWWINDOW | SWP_HIDEWINDOW));
+
+        release_win_data(data);   /* release the child before taking the root: win_data_mutex is
+                                   * recursive, so single-level lock scopes are a preference here,
+                                   * not a requirement */
+        if (moved && root && root != hwnd && (data = get_win_data(root)))
+        {
+            update_remote_layer_frame_for(data, hwnd, remote_layer_context_for(data, hwnd));
+            release_win_data(data);
+        }
+        return;
+    }
 
     if (data->on_screen)
     {
@@ -1687,6 +2091,10 @@ void macdrv_WindowPosChanged(HWND hwnd, HWND insert_after, HWND owner_hint, UINT
         }
     }
 
+    /* after the Cocoa frame is current, so hosted cross-process layers are placed against the
+     * content view's new bounds rather than the ones it is about to leave */
+    update_remote_layer_frames(data, &old_rects);
+
     if (fullscreen || fullscreen != data->fullscreen)
     {
         CGRect rect = (fullscreen && !EqualRect(&data->rects.window, &data->rects.visible)) ? cgrect_from_rect(data->rects.window) : CGRectZero;
@@ -1694,7 +2102,6 @@ void macdrv_WindowPosChanged(HWND hwnd, HWND insert_after, HWND owner_hint, UINT
         data->fullscreen = fullscreen;
     }
 
-done:
     release_win_data(data);
 }
 
@@ -1777,6 +2184,10 @@ void macdrv_window_frame_changed(HWND hwnd, const macdrv_event *event)
               data->rects.window.bottom - data->rects.window.top, width, height);
 
     being_dragged = data->drag_event != NULL;
+    /* Written on EVERY frame-changed event, so the bit self-heals rather than latching, and before
+     * NtUserSetRawWindowPos below -- that is what drives WindowPosChanged and therefore the root
+     * pass that reads it. */
+    data->in_live_resize = event->window_frame_changed.in_resize;
     release_win_data(data);
 
     if (event->window_frame_changed.fullscreen)
@@ -1959,7 +2370,17 @@ void macdrv_window_brought_forward(HWND hwnd)
  */
 void macdrv_window_resize_ended(HWND hwnd)
 {
+    struct macdrv_win_data *data;
+
     TRACE("hwnd %p\n", hwnd);
+    if ((data = get_win_data(hwnd)))
+    {
+        /* Clear the heuristic and re-derive every host from its child's real rect, so a stretched
+         * layer does not outlive the drag that justified it. */
+        data->in_live_resize = 0;
+        update_remote_layer_frames(data, NULL);
+        release_win_data(data);
+    }
     send_message(hwnd, WM_EXITSIZEMOVE, 0, 0);
 }
 
