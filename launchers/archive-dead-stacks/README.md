# Archived launchers — dead stacks (2026-08-22)

None of these work any more. Kept only for reference / salvage.

| script | stack | why dead |
|---|---|---|
| `Cities Skylines II (CrossOver).command` | CrossOver "Steam" bottle | **CrossOver licence EXPIRED 2026-08-21** — `steam.exe` won't start |
| `Paradox Launcher (CrossOver).command` | CrossOver bottle | same |
| `Cities Skylines II (Wine11).command` | `~/cs2-mac` Wine 11 + DXVK/MoltenVK | prefix **deleted from disk** |
| `Cities Skylines II.command` | `~/cs2-mac` | prefix **deleted from disk** |
| `CS2 LOGIN.command`, `CS2 Steam Login.command` | CDP/Steam login helpers for the Wine 11 CEF-black-screen workaround | prefix gone; the free stack's Steam logs in normally |
| `Show Steam.command` | Wine 11 helper | prefix gone |

**The one live launcher is `../Cities Skylines II (D3DMetal).command`**
(Kegworks/Sikarugir Wine 10 + D3DMetal, `~/Applications/S734M.app`).
Double-clickable shortcut: `~/Applications/Cities Skylines II.app` (+ Desktop alias).
