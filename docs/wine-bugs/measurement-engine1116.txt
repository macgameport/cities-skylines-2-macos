Engine: stock wine 11.16 + aquadran DXMT patch + PK DXMT binaries (self-built)
Plan: docs/plans/build-wine1116-dxmt-engine.md  ·  entries appended per gate as executed

== G0 (configure, pass 1: x86_64-only) — 2026-08-23 ==
invocation: docs/plans §4 verbatim (incl. the two ac_cv_lib_soname_* cache vars)
exit=0

include/config.h:
  #define SONAME_LIBFREETYPE "libfreetype.dylib"
  #define SONAME_LIBGNUTLS "libgnutls.dylib"
  #define SONAME_LIBMOLTENVK "libMoltenVK.dylib"
grep -c winemac Makefile: 242 (driver in the build)
configure warnings: zero schannel/gnutls lines; remaining notices are Linux-only subsystems
  (EGL/Wayland/sane/va/gphoto/pulse/FFmpeg/alsa/OSS/udev/capi) — expected on macOS.

DEVIATION FOUND AND FOLDED: first configure run (without the cache vars) captured polluted
sonames — a full otool -L line per lib ("\tlibfreetype.dylib (compatibility version 27.0.0...)").
Root cause: PK dylibs have pathless install names. Fix: pin ac_cv_lib_soname_{freetype,gnutls}.
Plan §4 updated in the same session.

G0: PASS

== Build + assembly (pass 1) — 2026-08-23 ==
gmake -j12 install: first run FAILED at fonts/ (sfnt2fon: "dyld: Library not loaded:
libfreetype.dylib" — build tools hit the pathless install name). Re-run with
DYLD_FALLBACK_LIBRARY_PATH=$PKLIB: exit 0. Deviation folded into plan §4.
Assembly per §5: wine64 symlink, 4 DXMT files overlaid (winemetal.so 51.8MB;
d3d11/dxgi/winemetal dlls), PK dylibs copied, gecko+mono copied, version file written.
Self-check: bin/wine64 --version = wine-11.16. OUR winemac.so EXPORTS _macdrv_functions
(dyld_info: 0x59560) — the aquadran shim is live; both DXMT binding paths available.
NOTE: no engine .so LINKS freetype/gnutls — wine dlopen()s the bare sonames at runtime, so
raw (non-launcher) runs need DYLD_FALLBACK_LIBRARY_PATH=<engine>/lib. Folded into plan §6.

== G1 (swapchain compositing) — 2026-08-23, display awake, hardened harness ==
CONTROL (daily engine, wine 11.0 + DXMT, prefix = daily wrapper):
  P1 (251,35,248) magenta — crop confirmed inside client area
  P2B (32,115,137) == P2B2 (32,115,137)  -> computed VERDICT: STALE (bug PRESENT)
CANDIDATE (CS2_WINE=/tmp/engine-1116/bin/wine64, fresh prefix ~/.e1116-prefix,
           DYLD_FALLBACK_LIBRARY_PATH=<engine>/lib, WINEESYNC/MSYNC=1):
  P1 (251,35,248) magenta
  P2B (168,22,15) != P2B2 (72,5,3) — red pulse ANIMATING on the older chain
  -> computed VERDICT: LIVE (fix PRESENT)
G1: PASS. Headline unknown SETTLED: PK's fork DXMT binaries (v0.80-17-g79f6279) bind and
present correctly on stock wine 11.16 — no WineForge fallback needed, no aquadran-path use
required at runtime (though the shim is compiled in and exported).

== G2 (raw Win32 errno) — 2026-08-23 ==
scripts/errtest.exe (compiled this session, x86_64-w64-mingw32-gcc -O2) on the new engine:
9/9 correct, 0 WRONG.
G2: PASS.

== G3 (Mono file IO, game's own Unity runtime) — 2026-08-23 ==
monohost.exe + filetest_net.exe, fresh prefix, game dir symlinked (live 10-patch mscorlib =
fshandle-only delta), exit 0: 44 OK / 7 expected exceptions / 0 garbage-errno lines.
Identical to the wine-11.0 and stock-11.15/11.16 baselines.
G3: PASS.

Pass-1 smoke ladder complete: G0+G1+G2+G3 all PASS. Pass 2 (i386,x86_64) building; G4 after.

== §7 pre-boot state — 2026-08-23 ==
Clone: CS2e1116.app created (97 GB APFS clone, cp exit 0, zero stderr); clone timestamp file
/tmp/cs2-clone-timestamp recorded for the §11.4 isolation measurement.
Clone patch state: cmp -l mscorlib vs .bak = exactly 4 bytes at 1468613-1468616 (fshandle) —
identical to the daily wrapper. 10-patch state confirmed, no patcher run.
Launcher fix synced via setup.sh (installed == repo, line 93 carries CS2_GAME_DIR; .bak kept).
V2 baseline: daily SceneFlow.log = <prefix>/.../Cities Skylines II/Logs/SceneFlow.log
(NOTE: Logs/ subdir — the CLAUDE.md table points at the parent dir), mtime Aug 23 19:56:40 2026.
V6 fixture note: Move It is ALREADY installed in the daily wrapper (MoveIt.coc present), so the
clone inherits it — V6 becomes unsubscribe -> resubscribe in-game, which exercises the
PrepareFolderForPatching recursive-delete path more directly than a fresh download.

== G4 (32-bit PE, pass 2) — 2026-08-23 ==
Pass-2 build (i386,x86_64): configure + gmake install exit 0; i386-windows = 1083 files.
DXMT re-overlaid on both arches after install (install refreshes stock dlls — §5 note).
errtest32.exe (i686-w64-mingw32-gcc, verified PE32 Intel 80386) on the new engine:
9/9 correct, 0 WRONG.
G4: PASS. No i386 waiver needed.

== Engine swap into the clone — 2026-08-23 ==
CS2e1116.app/Contents/SharedSupport/wine -> wine.pk11.0-BAK (rollback = one mv)
/tmp/engine-1116 copied in as wine/: bin/wine64 --version = wine-11.16,
version file = "wine stock 11.16 + DXMT (self-built)", 86 dylibs in lib/.
SMOKE LADDER COMPLETE: G0 G1 G2 G3 G4 ALL PASS, zero waivers.
Next: §7 V-session (human-driven game validation on the clone).

== V-session deviation #1 — 2026-08-23 ==
First launcher run on the clone wedged pre-login: the one-time 11.16 prefix update spawned the
wine-mono updater dialog invisibly (Steam -silent boot), rundll32 wine.inf sat at 0% CPU for
15+ min, launcher timed out with the (misleading) token-expired error. Recovery: wineserver -k
(clone-scoped) + controlled wineboot -u with WINEDLLOVERRIDES="mscoree=;mshtml=" -> completed in
15.6 s. Plan §7 updated to make the controlled pre-boot a standing step. Relaunching.

== V-session, live results so far — 2026-08-23 ~22:2x-22:4x ==
V1 PASS (after deviation #1's controlled prefix update): Steam boots + logs in on the new engine.
V6 PASS both halves, disk-verified: unsubscribe removed 74324_* (recursive delete clean, UIL dep
  correctly retained); resubscribe re-landed 74324_36 with full content, zero .downloading
  remnants. The path that originally forced the 6 errno patches works with 10 on stock 11.16.
V3 PASS: HUD 44.86 FPS / GPU 23.11 ms / interval 22.29 ms at the baseline settings & city —
  vs 40.9 FPS / 25.9-26.6 ms on wine 11.0. ~+10% FPS, ~-3 ms GPU.
PRESENTATION: "Direct" (green) in BOTH borderless and exclusive — 11.0 showed "Composited".
V5 MEASURED NEGATIVE: Game Mode = Off even in exclusive fullscreen. Hypothesis closed.
V4 CORE OBSERVATION (eyewitness + captures): click-outside in exclusive -> game self-minimizes
  (same first step as the old freeze trace) -> RESTORE COMES BACK LIVE, animation running.
  On 11.0 this exact cycle froze the render permanently. Repeated cycles in progress.
DXMT custom metric confirms fork build v0.80-17-g79f627 rendering.
PUBLISH DECISION (James): ship the build path -> scripts/build-engine-1116.sh + INSTALL §6.
Still pending: more V4 cycles (Cmd-Tab variant), V2 log diff, V7 second display, V8 save/load,
V9 borderless flip, V6b boot-clean, V10 registry grep, isolation measurement.

== PROMOTION — 2026-08-23 ~23:0x ==
Isolation measurement (§11.4): exactly 1 file in the old daily wrapper newer than the clone
timestamp — a JetBrains Toolbox .settings.json touched by setup.sh's idempotent repatch pass
(known repatch step); zero unexplained modifications. The G1 control run was plan-prescribed.
Rename swap executed with zero live processes:
  CS2dxmt11.app -> CS2dxmt11-pk110.app   (old wine 11.0 stack, parked; retirement decision
                                          after ~1 week per plan §8.1)
  CS2e1116.app  -> CS2dxmt11.app         (self-built stock 11.16 + DXMT, validated tonight,
                                          carries the session's saves + Move It state)
V10 dissolved by construction: the prefix's absolute font-registry paths now resolve into the
new wrapper, whose engine ships the fonts (marlett.ttf verified present).
Dock shortcut regenerated via make-shortcut.sh --hud: CS2_QUIET=1 CS2_HUD=1 -> canonical
launcher -> auto-detects CS2dxmt11.app = the new engine. No hand-edited files.
Next dock-icon launch doubles as V6b (boot with Move It enabled) + V2 (log diff).
